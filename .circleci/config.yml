version: 2

jobs:
  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh $SSH_USER@$SSH_HOST "git -C /opt/odoo clone $CIRCLE_REPOSITORY_URL "

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - deploy
